import { io } from "@tensorflow/tfjs-core";
import { FaceLandmarksDetection } from "./face";
export { FaceLandmarksDetection };
export type { NormalizedFace, FacePrediction } from "./face";
export interface OptionTypes {
    modelUrl: string;
    maxFaces?: number;
    inputWidth?: number;
    inputHeight?: number;
    iouThreshold?: number;
    scoreThreshold?: number;
    loadOptions?: io.LoadOptions;
}
export declare const load: ({ maxFaces, inputWidth, inputHeight, iouThreshold, scoreThreshold, loadOptions, modelUrl, }: OptionTypes) => Promise<FaceLandmarksDetection>;
